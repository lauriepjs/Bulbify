import React from "react";
import Navigation from "../components/Navigation";

const Join = () => {
    return (
        <div>
            <Navigation />
        </div>
    );
};

export default Join;
